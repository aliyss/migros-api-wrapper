import { describe, expect, test } from '@jest/globals';
// import { MigrosAPI } from "../src";

describe('Check for Migros Cookies', () => {
	test('Start login process and retrieve Cookies', async () => {
		/*
		const response = await MigrosAPI.account.security.getOptions({
			["CSRF"]: ".",
			["JSESSIONID"]: ".",
			["MDID"]: ".",
			["MLRM"]: ".",
			["MTID"]: ".",
			["TS012f1684"]: ".",
			["__VCAP_ID__"]: ".",
			["hl"]: "."
		})
		expect(Object.prototype.hasOwnProperty.call(response.body, "activeAccount2fa")).toBe(true);
		expect(Object.prototype.hasOwnProperty.call(response.body, "notificationNewDeviceLogin")).toBe(true);
		 */
		expect(null).toBe(null)
	});
});