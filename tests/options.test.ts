import { describe, expect, test } from '@jest/globals';
// import MigrosAPI from "../src";

describe('Check for Migros Cookies', () => {
	test('Start login process and retrieve Cookies', async () => {
		/*
		const response = await MigrosAPI.security.options.get({
			["CSRF"]: ".",
			["JSESSIONID"]: ".",
			["MDID"]: ".",
			["MLRM"]: ".",
			["MTID"]: ".",
			["TS012f1684"]: ".",
			["__VCAP_ID__"]: ".",
			["hl"]: "."
		})
		expect(Object.prototype.hasOwnProperty.call(response, "activeAccount2fa")).toBe(true);
		expect(Object.prototype.hasOwnProperty.call(response, "notificationNewDeviceLogin")).toBe(true);
		 */
		expect(null).toBe(null)
	});
});