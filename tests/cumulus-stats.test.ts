import { describe, expect, test } from '@jest/globals';
// import MigrosAPI from "../src";

describe('Check for Migros Cumulus Stats', () => {
	test('Retrieve Cumulus Stats', async () => {
		/*
		const response = await MigrosAPI.account.cumulus.getCumulusStats({
			["CSRF"]: ".",
			["JSESSIONID"]: ".",
			["MDID"]: ".",
			["MLRM"]: ".",
			["MTID"]: ".",
			["TS012f1684"]: ".",
			["__VCAP_ID__"]: ".",
			["hl"]: "."
		})
		expect(Object.prototype.hasOwnProperty.call(response.body, "available")).toBe(true);
		expect(Object.prototype.hasOwnProperty.call(response.body, "statusCumulusMissing")).toBe(true);
		 */
		expect(null).toBe(null)
	});
});